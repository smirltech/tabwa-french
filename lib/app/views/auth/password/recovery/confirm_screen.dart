import 'package:flutter/material.dart';

class PasswordRecoveryConfirmScreen extends StatelessWidget {
  const PasswordRecoveryConfirmScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
