import 'package:flutter/material.dart';

class PasswordRecoveryRequestScreen extends StatelessWidget {
  const PasswordRecoveryRequestScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
