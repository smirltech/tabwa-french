import 'package:flutter/material.dart';

class PasswordRecoveryResetScreen extends StatelessWidget {
  const PasswordRecoveryResetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
